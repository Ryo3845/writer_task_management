<header>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <% if logged_in? %>
              <h1><%= link_to image_tag("WriterTaskLogo", alt: "WriterTaskロゴ", width: 40, height: 40, class: "nav-link active", aria: { current: "page" } ), projects_path %></h1>
            <% else %>
              <h1><%= link_to image_tag("WriterTaskLogo", alt: "WriterTaskロゴ", width: 40, height: 40), root_path %></h1>
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              メニュー
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="#">メニュー1</a></li>
              <li><a class="dropdown-item" href="#">メニュー2</a></li>
            </ul>
          </li>
        </ul>
        <div><%= current_user.name %>さん</div>
        <div><%= image_tag "account_circle.png", alt: "アカウント編集ロゴ", width: 30, height: 30 %></div>
      </div>
    </div>
  </nav>
</header>
<div id="logo-container">
</div>